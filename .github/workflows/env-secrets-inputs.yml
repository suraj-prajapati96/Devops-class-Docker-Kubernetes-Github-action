name: Demo CI ENV

on:
    # push
    workflow_dispatch

jobs:
    build:
        runs-on: ubuntu-latest
        
        steps:
           - name: Say Hello
             run: echo "Hello from Second CI workflow!"

           - name: Install
             env:
                APP_ENV: demo
             run: echo "Environment $APP_ENV"
